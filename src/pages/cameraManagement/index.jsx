import React from 'react';
import PropTypes from 'prop-types';
import ListCamera from './ListCamera';

function CameraManagement() {
  return <ListCamera />;
}

CameraManagement.propTypes = {};

export default CameraManagement;
